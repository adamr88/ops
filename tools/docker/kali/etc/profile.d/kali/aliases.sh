# clipboard
alias pbcopy='xsel --clipboard --input'
alias pbpaste='xsel --clipboard --output'

# editor
alias vi=vim
alias vim=nvim

# preserve scrollback
alias clear='clear -x'
alias tmux-save-pane='tmux capture-pane -pS -'  # > capture.txt
